import axios from 'axios'
import ApiServices from './ApiServices'
import CookieService from './CookieService'

interface Credentials {
    email: string,
    password: string
}

class AuthServices {
    async authenticateAgentAccount(credential: Credentials) {
        
    }
}